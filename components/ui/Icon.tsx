// import React from 'react';
// import { Pressable, StyleSheet } from 'react-native';

// import { COLORS, SIZES, Icon as ICON } from '../../constants';

// interface IconProps {
//   name: string;
//   color?: string;
//   size?: number;
//   style?: object;
//   onPress?: () => void;
//   testID?: string;
// }

// const Icon = ({ name, color, size, style, onPress, testID }: IconProps) => {
//   return (
//     <Pressable testID={testID} onPress={onPress} style={style}>
//       <ICON name={name} size={size} color={color} style={style} />
//     </Pressable>
//   );
// };

// export default Icon;

// const styles = StyleSheet.create({});
